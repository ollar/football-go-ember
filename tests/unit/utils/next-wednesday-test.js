import nextWednesday from 'football-go-ember/utils/next-wednesday';
import { module, test } from 'qunit';

module('Unit | Utility | next wednesday');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nextWednesday();
  assert.ok(result);
});
